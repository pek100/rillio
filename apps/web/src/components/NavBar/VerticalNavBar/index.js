// Copyright (C) 2017-2024 Smart code 203358507

// Unwrap the ES default so both `require(...)` and `import X from ...` call sites
// receive the component directly.
module.exports = require('./VerticalNavBar').default;
